# Gradle Wrapper

The package includes the Gradle 8.13 wrapper configuration and `gradle/wrapper/gradle-wrapper.jar`. A release build still requires an Android SDK, release signing configuration and clean CI/device verification.
